# ─────────────────────────────────────────────────────────────
#  ArcJournal — Environment Variables
#  Copy to .env (never commit .env to git)
# ─────────────────────────────────────────────────────────────

# ── MongoDB ───────────────────────────────────────────────────
# Local:
MONGODB_URI=mongodb://localhost:27017/arcjournal
# Railway MongoDB plugin (copy from Railway dashboard):
# MONGODB_URI=mongodb://mongo:<password>@<host>:<port>/arcjournal

# ── Auth ──────────────────────────────────────────────────────
JWT_SECRET=change_this_to_a_long_random_secret_string_min_32_chars
JWT_EXPIRES_IN=30d

# ── Server ────────────────────────────────────────────────────
PORT=3001

# ── CORS (comma-separated, add your Railway URL when deployed) ─
# CORS_ORIGINS=http://localhost:5173,http://localhost:5174

# ── Client: API URL ──────────────────────────────────────────
# Dev — leave as-is
VITE_API_URL=http://localhost:3001
# Production (set in client/.env.production or at build time)
# VITE_API_URL=https://your-arcjournal-server.up.railway.app

# Railway TOKEN
RAILWAY_TOKEN=enter_your_railway_token_here
